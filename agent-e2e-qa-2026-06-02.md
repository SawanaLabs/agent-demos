# Agent Demo E2E QA - 2026-06-02

Scope: end-to-end smoke QA for ready demo pages in `apps/web`, using the deployed production domain in the browser.

Rules:

- Read-only with respect to code/config. The browser may create visitor-scoped chats, cookies, localStorage, DB rows, and model usage while sending messages.
- Exercise core behavior by clicking sample suggestions first, then sending follow-up turns such as "continue" where the demo supports chat continuation.
- Record reproducible failures with exact URL, steps, expected behavior, actual behavior, visible error text, relevant network/console details, and severity.
- Avoid destructive UI actions such as deleting chats, clearing history, or uploading private/local files.

## Environment

- Date: 2026-06-02
- Local URL: `http://localhost:3000`
- Initial local server: existing `next-server (v16.1.6)` on port 3000
- Local browser E2E note: the Browser tool blocked direct `localhost` navigation in this session, so UI click-through QA was run against the production custom domain.
- Production URL tested in browser: `https://agent-demos.hsawana9.com`
- Latest Vercel deployment URL: `https://ai-sdk-6-ai-elements-demos-3tqjq6voz-multicul-lives-projects.vercel.app`
- Existing unrelated worktree change: `docs/planning/work-roadmap.md`

## Production Retest - 2026-06-03

- Date: 2026-06-03 09:15 CST
- Production URL retested: `https://agent-demos.hsawana9.com`
- Method: headless Chrome against the production custom domain. No code or config changes were made. Browser actions created visitor-scoped chats, threads, memories, and model usage.
- Production HTTP check: root returned `200` from Vercel with `x-vercel-cache: HIT`.
- Worktree note: unrelated in-progress changes were present in `CONTEXT.md`, `docs/planning/work-roadmap.md`, `docs/quality/DOCS.md`, `docs/quality/index.md`, and `docs/quality/resource-abuse-privacy-review.md`; this QA pass did not modify them.

| QA | Current status | Retest evidence |
| --- | --- | --- |
| QA-007 | Resolved | `openai-agents-sdk-demo` now shows `Ready`, no `AI_GATEWAY_API_KEY is missing`, sample prompt completes, and the composer is enabled again after the run. |
| QA-008 | Resolved for original setup blocker; new issue logged as QA-014 | `object-generation` now shows `Ready`, text input and samples are enabled, and a structured object reaches `Object ready`. The record persistence panel then reports a separate 404. |
| QA-009 | Resolved | `customer-memory-agent` now shows `Ready`, no missing AI Gateway or database message, `New` creates a Brightfield thread, sample prompt runs `manageCustomerMemory`, and saved memory count updates to 1. |
| QA-010 | Resolved | `streaming-chat-shell` now shows `Ready`; sample prompt responds, replay reconstructs text with parsed events, and a typed follow-up is accepted. |
| QA-011 | Resolved | `loop-agent` now shows `Ready`; HITL sample reaches approval, `Approve` produces the escalation response, and a typed follow-up is accepted. |
| QA-012 | Still open, updated repro | Setup/composer lock is fixed. A focused replay did not reproduce the source/eval contradiction on the successful run; instead 2 of 3 fresh runs executed `web_search` and then failed before producing a final answer. |
| QA-013 | Resolved for original lock/dead-end | `multimodal-chatbot` now shows `Ready`; `Attach`, file inputs, composer, samples, and typed follow-up are enabled. The PDF sample still asks for an upload when no PDF exists, but the visible upload path is available. |
| QA-014 | Pending post-deploy recheck | Production still returns recorded-output 404, but the same POST plus record lookup passes on the current local server. Recheck after the latest local fixes are deployed before treating this as an active production bug. |

## QA Items

### QA-007 - `openai-agents-sdk-demo` production page blocks all text-message paths because `AI_GATEWAY_API_KEY` is missing

- Severity: P0
- Status: Resolved on 2026-06-03 production retest.
- URL: `https://agent-demos.hsawana9.com/demos/openai-agents-sdk-demo`
- Environment: production domain, Browser session on 2026-06-02
- 2026-06-03 retest:
  - Page now shows `Ready` and no `AI_GATEWAY_API_KEY is missing` setup state.
  - Textarea, submit, and all three text suggestions are enabled before the run.
  - Clicking `Explain how this demo bridges the OpenAI Agents SDK run into AI SDK UI messages.` produced an assistant response about converting the Agents SDK runtime event stream into AI SDK UI messages.
  - The final state had no `Assistant response failed`, no failed requests, and the composer returned to enabled.
- Steps:
  1. Open `/demos/openai-agents-sdk-demo`.
  2. Observe setup/runtime state.
  3. Try to use the chat suggestions or manual composer.
- Expected:
  - A ready deployed demo should let the user send a prompt through the OpenAI Agents SDK backend and render AI SDK UI messages.
  - If production is intentionally missing required credentials, all interactive text entry should remain blocked with a clear setup state.
- Actual:
  - Page shows `Setup required` and `AI_GATEWAY_API_KEY is missing. Add it to apps/web/.env.local using the contract in apps/web/.env.example.`
  - Textarea and `Submit` are disabled.
  - All three text suggestions are disabled:
    - `Explain how this demo bridges the OpenAI Agents SDK run into AI SDK UI messages.`
    - `Run a short planning answer that uses the configured guide coverage and model profile.`
    - `Summarize what the demo proves about tools, handoffs, and tracing in one response.`
  - No text message can be sent, so tools, handoffs, approvals, tracing, MCP, sandbox, and guardrail behavior cannot be exercised through the chat.
- Visible evidence:
  - Runtime inspector shows many sections in `setup-required` state, including voice agents, guardrails, tools, and guide coverage runs.
  - `Last response` is `Not available yet`; session id is `Not created yet`.
- Repro notes:
  - This page blocks text entry consistently, unlike several other setup-required demos.
  - The enabled `Connect` button belongs to the voice lane and was not used in this text-message QA pass because it would require real-time/microphone behavior.

### QA-008 - `object-generation` production page blocks all text-message paths because `AI_GATEWAY_API_KEY` is missing

- Severity: P0
- Status: Resolved for the original setup blocker on 2026-06-03 production retest. See QA-014 for the new record persistence failure found during the same run.
- URL: `https://agent-demos.hsawana9.com/demos/object-generation`
- Environment: production domain, Browser session on 2026-06-02
- 2026-06-03 retest:
  - Page now shows `Ready` and no `AI_GATEWAY_API_KEY is missing` setup state.
  - Textarea, submit, `Attach`, and all sample suggestions are enabled before the run.
  - Clicking `Generate a launch-risk object for this pricing page draft and flag unsupported claims.` streamed a structured `Object ready` result with decision, risk score, findings, evidence, recommended action, and open questions.
  - The original blocker is gone. A separate `RECORDED OUTPUT` failure was visible after the successful object generation and is tracked as QA-014.
- Steps:
  1. Open `/demos/object-generation`.
  2. Observe setup state.
  3. Try to use the sample suggestions or manual composer.
- Expected:
  - A ready deployed demo should let the user send text and stream a structured object into the assistant message.
  - If credentials are intentionally missing, the page should clearly block the text path.
- Actual:
  - Page shows `Setup required` and `AI_GATEWAY_API_KEY is missing. The demo can render, but object generation requests will fail until it is configured.`
  - Textarea and `Submit` are disabled.
  - All three sample suggestions are disabled.
  - The `Attach` button remains enabled, but the text/object-generation request path is unavailable.
- Visible evidence:
  - Runtime panel shows `RUNTIME Setup required`.
  - Output shape and input contract are visible, but no request can be started through text.
- Repro notes:
  - Attach/upload was not used in this read-only QA pass.

### QA-009 - `customer-memory-agent` production page cannot start memory threads because model and database configuration are missing

- Severity: P0
- Status: Resolved on 2026-06-03 production retest.
- URL: `https://agent-demos.hsawana9.com/demos/customer-memory-agent`
- Environment: production domain, Browser session on 2026-06-02
- 2026-06-03 retest:
  - Page now shows `Ready` and no missing AI Gateway or `DATABASE_URL` text.
  - Customer buttons, `New`, textarea, submit, and sample suggestions are enabled after initial thread loading.
  - Clicking `New` created a new Brightfield memory thread.
  - Clicking `Remember that Brightfield Health requires compliance-safe rollout language and wants every customer-facing update to stay plain and factual.` completed `manageCustomerMemory`.
  - The final UI showed `2 messages`, `1 memories`, `1 events`, and a saved memory titled `Compliance-safe rollout language constraint for Brightfield Health`.
- Steps:
  1. Open `/demos/customer-memory-agent`.
  2. Observe customer list, thread list, setup banner, and composer state.
  3. Try to create or use a memory chat.
- Expected:
  - A ready deployed demo should let the user chat with the writable visitor-scoped customer, invoke the memory lifecycle tool, persist memories, and restore threads.
  - If persistence is unavailable, thread creation and message entry should remain blocked with a clear setup state.
- Actual:
  - Page shows:
    `AI_GATEWAY_API_KEY is missing. The demo can render, but memory-agent chat requests will fail until it is configured. DATABASE_URL is missing. The memory and persistence agent requires a writable Postgres database.`
  - Customer account buttons are selectable, but `New`, textarea, `Submit`, and sample suggestions are disabled.
  - `No threads loaded yet`, `0 threads`, `0 messages`, `0 memories`, and `0 events` are visible.
  - The core memory/persistence workflow cannot be started.
- Visible evidence:
  - Writable account `Brightfield Health` is visible, but no chat can be created.
  - Saved memories panel says no customer memories are active yet.
- Repro notes:
  - This is a hard production configuration failure for the customer memory demo.

### QA-010 - `streaming-chat-shell` replay works, but setup-required state blocks manual continuation

- Severity: P1
- Status: Resolved on 2026-06-03 production retest.
- URL: `https://agent-demos.hsawana9.com/demos/streaming-chat-shell`
- Environment: production domain, Browser session on 2026-06-02
- 2026-06-03 retest:
  - Page now shows `Ready` and no `AI_GATEWAY_API_KEY is missing` setup state.
  - Composer and sample suggestions are enabled.
  - Clicking `Explain why replaying the same prompt turn is useful for developer debugging.` produced an assistant response.
  - `Replay prompt turn` produced `RECONSTRUCTED TEXT` and parsed replay events.
  - A typed follow-up, `Continue with one concrete debugging scenario.`, was accepted and added a second user prompt to the replay trace.
- Steps:
  1. Open `/demos/streaming-chat-shell`.
  2. Observe setup banner and disabled composer.
  3. Click the sample suggestion `Explain why replaying the same prompt turn is useful for developer debugging.`
  4. Wait for the assistant response.
  5. Expand `Replayable developer trace`.
  6. Click `Replay prompt turn`.
  7. Try to send a manual continuation.
- Expected:
  - If the model path is usable enough to answer and replay, the setup banner should not say chat requests will fail and the composer should allow a second typed turn.
  - Replay should reconstruct the same prompt turn and expose parsed streaming events.
- Actual:
  - Page shows `Setup required` and `AI_GATEWAY_API_KEY is missing. The demo can render, but chat requests will fail until it is configured.`
  - Textarea and `Submit` are disabled, but sample suggestions remain enabled.
  - Clicking the first sample produces an assistant response.
  - Opening the trace shows `2 thread messages`, `1 user prompts`, and the correlated user prompt.
  - Clicking `Replay prompt turn` works: the panel shows `61 events` and a `RECONSTRUCTED TEXT` answer.
  - Textarea remains disabled after the successful answer and replay, so the user cannot send a typed continuation.
- Visible evidence:
  - Replay panel shows `Correlated user prompt: "Explain why replaying the same prompt turn is useful for developer debugging."`
  - Replay panel shows `RECONSTRUCTED TEXT` with a separate replayed answer.
- Repro notes:
  - Core replay trace behavior is usable.
  - The broken part is the setup/composer gating contract, same family as the other partially working setup-required pages.

### QA-011 - `loop-agent` tool loop and HITL approval work, but setup-required state blocks typed follow-up

- Severity: P1
- Status: Resolved on 2026-06-03 production retest.
- URL: `https://agent-demos.hsawana9.com/demos/loop-agent`
- Environment: production domain, Browser session on 2026-06-02
- 2026-06-03 retest:
  - Page now shows `Ready` and no `AI_GATEWAY_API_KEY is missing` setup state.
  - Composer and sample suggestions are enabled.
  - Clicking `Triage CASE-1842, request human approval before escalation, then finalize.` reached the human approval checkpoint with `Reject` and `Approve`.
  - Clicking `Approve` produced the escalation response for Northstar Analytics / CASE-1842.
  - A typed follow-up, `Continue with one concrete reason for the escalation.`, was accepted and answered in the same thread.
- Steps:
  1. Open `/demos/loop-agent`.
  2. Observe setup banner and disabled composer.
  3. Click `Triage CASE-1842, request human approval before escalation, then finalize.`
  4. Wait for the tool loop to reach the human approval checkpoint.
  5. Click `Approve`.
  6. Wait for the final agent response.
  7. Try to send a manual follow-up.
- Expected:
  - Tool loop should run independent lookups, dependent SLA decision, HITL approval, then final escalation response.
  - After the loop completes, the user should be able to continue the chat manually.
  - Setup banner should match the actual runtime availability.
- Actual:
  - Page shows `Setup required` and `AI_GATEWAY_API_KEY is missing. The demo can render, but chat requests will fail until it is configured.`
  - Textarea and `Submit` are disabled, but sample suggestions remain enabled.
  - Clicking the HITL sample works: four `Support Triage Tool Completed` chips appear before approval.
  - The page renders a human approval checkpoint with `Reject` and `Approve`.
  - Clicking `Approve` works and produces the final response: `The support case CASE-1842 for Northstar Analytics is escalated to priority support...`
  - After completion, the composer remains disabled, so a manual follow-up cannot be sent.
- Visible evidence:
  - Tool sequence lines include `getCustomerProfile`, `listRecentTickets`, `checkEntitlement`, `calculateSlaRisk`, and `requestHumanApproval`.
  - Approval state changes to `Approved. The agent can execute the escalation handoff.`
- Repro notes:
  - Core tool-loop and human-approval behavior is usable.
  - The failure is the same setup/composer gating mismatch as other partially working pages.

### QA-012 - `trace-eval-agent` production research runs are unstable after `web_search`

- Severity: P1
- Status: Still open after 2026-06-03 focused production replay. The setup/composer lock is fixed. The earlier source/eval contradiction did not reproduce on the successful replay, but production still has unstable run/eval outcomes.
- URL: `https://agent-demos.hsawana9.com/demos/trace-eval-agent`
- Environment: production domain, Browser session on 2026-06-02
- 2026-06-03 retest:
  - Page now shows `Ready`, no `AI_GATEWAY_API_KEY is missing` setup state, and the composer remains enabled after the first run.
  - Clicking `Pick a tracing + eval stack for a Next.js AI agent` completed a live research run and rendered source URL buttons, including:
    - `https://langfuse.com/blog/2024-07-ai-agent-observability-with-langfuse`
    - `https://github.com/genai-rs/opentelemetry-langfuse`
    - `https://www.braintrust.dev/articles/agent-observability-complete-guide-2026`
    - `https://www.digitalapplied.com/blog/agent-observability-2026-evals-traces-cost-guide`
  - Eval still reported `Score 75%` / `Judge 50%`.
  - Judge text still said no actual source links are attached in run metadata or final output, while the chat UI rendered source URL buttons.
  - A typed follow-up, `Continue with one concrete source-backed recommendation.`, was accepted, but the judge again said source content was absent from run metadata.
  - The browser recorded aborted `POST /api/demos/trace-eval-agent/evaluate/stream` requests during the follow-up eval stream.
- 2026-06-03 focused replay:
  - Method: headless Chrome against production custom domain, three fresh page runs, clicking `Pick a tracing + eval stack for a Next.js AI agent` each time.
  - Attempt 1 and attempt 2 both returned `200` `text/event-stream` from `POST /api/demos/trace-eval-agent`, rendered `web_search Completed`, then stopped at `The run completed without a final answer. Retry the run before evaluating quality.`
  - Those failed attempts made no `POST /api/demos/trace-eval-agent/evaluate/stream` request, showed `Judge Not run`, and reported `Usage 0 tokens`.
  - Attempt 3 completed successfully with a final answer, `4 sources`, `4 passed`, `Score 100%`, `Judge 94%`, and `Usage 6328 tokens`.
  - Attempt 3 sent snapshot sources to `/api/demos/trace-eval-agent/evaluate/stream`; captured request bodies included four source entries for Langfuse, Braintrust OpenTelemetry, Next.js OpenTelemetry, and Langfuse GitHub.
  - One aborted `/evaluate/stream` POST was observed during the successful attempt after another successful `200` eval stream response.
  - Conclusion: source serialization into the eval snapshot worked on the successful replay, so the source/eval contradiction is not currently deterministic. The stable reproduced failure is `web_search` completion followed by no final assistant answer.
- Steps:
  1. Open `/demos/trace-eval-agent`.
  2. Click `Pick a tracing + eval stack for a Next.js AI agent`.
  3. Wait for either the research response and eval panel or the `Run completed without answer` state.
  4. Inspect execution trace, current-conversation gates, LLM judge, sources, and composer state.
  5. Repeat in a fresh page session to distinguish intermittent model/tool failures from deterministic eval bugs.
- Expected:
  - The research run should execute `web_search`, render sources, populate execution trace, and score the run consistently.
  - If the final answer exposes at least two URL sources, source-coverage gates and judge text should not claim that no source links are visible.
  - If the model/tool stream cannot produce a final answer, the run should expose a provider/tool failure reason rather than ending as a successful `web_search` trace with zero answer tokens.
  - The user should be able to send a typed follow-up after the first run.
- 2026-06-02 original actual:
  - Page shows `Setup required` and `AI_GATEWAY_API_KEY is missing. The demo can render, but trace and eval requests will fail until it is configured.`
  - Textarea and `Submit` are disabled, but sample suggestions remain enabled.
  - Clicking the sample works and runs `web_search Completed`.
  - The assistant answer includes a `Sources:` section with multiple visible URLs, and the UI renders source URL buttons including Langfuse, Braintrust, Next.js OpenTelemetry, and a 2026 observability guide.
  - Execution trace populates with model run, `web_search`, latency, and token usage.
  - Eval panel reports `3 passed`, `1 failed`, `Gate score 75%`, and `Judge score 36%`.
  - LLM judge says the answer `critically misses exposing any source URLs` and lists `Sources attached` as a hard gate failure, contradicting the visible answer/source buttons.
  - Composer remains disabled after completion, so a manual continuation cannot be sent.
- 2026-06-03 focused replay actual:
  - Attempt 1 screenshot: `/tmp/trace-eval-qa-012-repro-attempt-1.png`.
  - Attempt 2 screenshot: `/tmp/trace-eval-qa-012-repro-attempt-2.png`.
  - Attempt 3 screenshot: `/tmp/trace-eval-qa-012-repro-attempt-3.png`.
  - The successful attempt did not reproduce missing source metadata; the captured eval snapshot contained four sources.
- Visible evidence:
  - Visible source URLs include `https://langfuse.com/blog/2024-07-ai-agent-observability-with-langfuse`, `https://www.braintrust.dev/docs/integrations/sdk-integrations/opentelemetry`, and `https://nextjs.org/docs/app/guides/open-telemetry`.
  - Eval text says: `No source links are provided despite claims referencing specific URLs...`
- Repro notes:
  - Core trace and eval surfaces are usable.
  - Source detection/eval input shaping appears inconsistent with the rendered assistant message or tool-result source parts.
  - Setup/composer gating mismatch is also present.

### QA-013 - `multimodal-chatbot` sample image path works, but visible upload/continuation UI is locked

- Severity: P1
- Status: Resolved for the original upload/continuation lock on 2026-06-03 production retest.
- URL: `https://agent-demos.hsawana9.com/demos/multimodal-chatbot`
- Environment: production domain, Browser session on 2026-06-02
- 2026-06-03 retest:
  - Page now shows `Ready` and no `AI_GATEWAY_API_KEY is missing` setup state.
  - `Attach`, file inputs, textarea, submit, and sample suggestions are enabled.
  - Clicking `What stands out in this image?` produced an image-grounded answer and left the composer enabled.
  - A typed follow-up, `Continue with one concrete visual detail.`, was accepted.
  - Reloading and clicking `Summarize the uploaded PDF in three bullets.` still produces a no-PDF answer when no PDF is attached, but `Attach`, file input, textarea, and submit remain enabled, so the user can satisfy the request through the visible UI.
  - No private/local file was uploaded in this retest.
- Steps:
  1. Open `/demos/multimodal-chatbot`.
  2. Observe setup banner, attachment state, and composer state.
  3. Click `What stands out in this image?`
  4. Wait for the assistant response.
  5. Reload the page.
  6. Click `Summarize the uploaded PDF in three bullets.`
  7. Try to attach a file or continue through the visible UI.
- Expected:
  - A multimodal demo should expose a usable visible attachment flow when asking about user-provided files.
  - Sample suggestions should either attach explicit visible demo fixtures or stay disabled until a file exists.
  - After a sample turn, the user should be able to upload/attach and continue.
- Actual:
  - Page shows `Setup required` and `AI_GATEWAY_API_KEY is missing. The demo can render, but multimodal chat requests will fail until it is configured.`
  - Visible `Attach`, textarea, and `Submit` controls are disabled, but sample suggestions remain enabled.
  - Clicking `What stands out in this image?` produces an image-grounded answer about bees and honeycomb, even though no image is visible in the chat or file list.
  - Reloading and clicking `Summarize the uploaded PDF in three bullets.` produces: `I currently don't see any PDF uploaded. Could you please upload the file so I can help summarize it?`
  - The visible `Attach` button and composer remain disabled, so the user cannot satisfy that request through normal UI.
- Visible evidence:
  - After the image suggestion, the answer says: `The most striking feature in this image is the large quantity of bees densely clustered around a piece of honeycomb.`
  - After the PDF suggestion, the answer says no PDF is uploaded.
  - Runtime panel still advertises accepted media `application/pdf` and `image/*`.
- Repro notes:
  - Image sample likely carries a hidden fixture or request-side sample attachment, but the fixture is not visible to the user.
  - PDF sample lacks an equivalent fixture and dead-ends because upload controls are disabled.

### QA-014 - `object-generation` creates an object, but recorded-output lookup reports 404

- Severity: P1
- Status: Pending post-deploy recheck after 2026-06-03 investigation.
- URL: `https://agent-demos.hsawana9.com/demos/object-generation`
- Environment: production domain and local dev server retest on 2026-06-03
- Steps:
  1. Open `/demos/object-generation`.
  2. Click `Generate a launch-risk object for this pricing page draft and flag unsupported claims.`
  3. Wait for the structured object to finish.
- Expected:
  - After the structured object reaches `Object ready`, the recorded output panel should either show the saved object record or omit the panel if no persistence record is meant to exist.
- Actual:
  - The object generation itself succeeds and shows `Object ready`.
  - The page also shows:
    `RECORDED OUTPUT`
    `Record failed`
    `{"error":"No object-generation record found for 16f0b8a3-cdd4-4ebe-bed3-8e4ec8f47c9f."}`
  - Browser console recorded a failed resource with HTTP 404.
- Repro notes:
  - This is separate from QA-008. The original AI Gateway setup blocker is fixed.
  - 2026-06-03 API-level production replay still reproduced the 404: `POST /api/demos/object-generation` returned `200` with `x-object-generation-record-id: 3b38704a-2841-4b81-b9b2-c36dde152c87`, but repeated `GET /api/demos/object-generation/records?recordId=3b38704a-2841-4b81-b9b2-c36dde152c87` checks returned `404`, including delayed checks after 5s and 15s.
  - The same API-level replay passed on the current local dev server: `POST /api/demos/object-generation` returned `200` with `x-object-generation-record-id: 6cda4ff9-4ac0-4105-b283-bc6e86927143`, and the matching records lookup returned `200`.
  - Because local did not reproduce, recheck production after the current local code is deployed before treating this as an active persistence or lookup bug.

## Coverage Log

| Demo | URL | Actions | Result | Notes |
| --- | --- | --- | --- | --- |
| Foundation Chat | `/demos/foundation-chat` | Setup-required entry-path contract reproduced in component test and registry sync | Fixed locally | Sample suggestions now share the disabled composer state when chat setup is unavailable. |
| RAG Chatbot | `/demos/rag-chatbot` | Clicked `What does the manual say about the NASA logotype?`, then sent `Continue with one concrete source-backed detail.` | Pass | First response showed `Used 4 sources` and `Knowledge Base Retrieval Completed`; continuation completed, but response time exceeded one 70s browser-script window before final state was visible |
| Ultra Chatbot Agent | `/demos/ultra-chatbot-agent` | Reproduced sandbox toolbox setup failure in server contract tests after approval state is enabled | Fixed locally | Post-approval toolbox failures now return concrete JSON errors and close opened MCP clients; real provider sandbox execution still depends on Vercel Sandbox credentials. |
| Sandbox Workspace Agent | `/demos/sandbox-agent` | Reproduced setup-required sample-button bypass in component contract tests | Fixed locally | Sample prompts now share the disabled composer state when sandbox-agent setup is unavailable; server setup still fail-fast checks AI Gateway and Vercel Sandbox credentials. |
| Skills Builder Agent | `/demos/skills-agent` | Reproduced setup-required sample/retry bypass in component contract tests | Fixed locally | Sample prompts and retry now share the disabled composer state, with handlers guarded while setup is unavailable or a turn is busy. |
| MCP Runtime Doctor Agent | `/demos/mcp-agent` | Reproduced stale setup-state and setup-unavailable entry bypass in route/UI contract tests | Fixed locally | Page now force-renders dynamically so runtime setup state matches the API route; sample and retry share the disabled composer state when setup is unavailable. |
| LangGraph Agent | `/demos/langgraph-agent` | Reproduced stale remote setup-state and setup-unavailable entry bypass in route/UI contract tests | Fixed locally | Page now force-renders dynamically so runtime setup state matches the API route; empty state, sample, and retry share the setup-unavailable state. |
| OpenAI Agents SDK Demo | `/demos/openai-agents-sdk-demo` | Retested production: clicked bridge sample and waited for full response | Resolved | See QA-007 |
| Object Generation | `/demos/object-generation` | Retested production and current local API path with launch-risk sample | Pending post-deploy recheck | Original setup blocker resolved. Production still returns recorded-output 404, but current local server returns the matching ready record. See QA-008 and QA-014 |
| Customer Memory Agent | `/demos/customer-memory-agent` | Retested production: created Brightfield thread, sent memory sample, inspected saved memory | Resolved | See QA-009 |
| Persistent & Resume Agent | `/demos/persistent-agent` | Clicked checklist sample, confirmed route promotion to `/demos/persistent-agent/8e7cebaf-3908-4952-9137-7ea5a9286725`, sent continuation, refreshed restored route | Pass | First and second turns persisted; after reload session state changed to `Restored route` and recent chat appeared |
| Streaming Chat Shell | `/demos/streaming-chat-shell` | Retested production: clicked replay sample, replayed prompt turn, sent typed follow-up | Resolved | See QA-010 |
| Loop Agent | `/demos/loop-agent` | Retested production: clicked HITL triage sample, approved escalation, sent typed follow-up | Resolved | See QA-011 |
| Trace and Eval Agent | `/demos/trace-eval-agent` | Focused production replay: clicked research-stack sample in three fresh page sessions | Partial fail | Setup/composer lock resolved. Successful replay carried four eval sources; 2 of 3 runs completed `web_search` but produced no final answer. See QA-012 |
| Multi-modal Chatbot | `/demos/multimodal-chatbot` | Retested production: clicked image sample, sent typed follow-up, reloaded and clicked PDF sample without upload | Resolved | Original visible upload/continuation lock is fixed; no private/local file was uploaded. See QA-013 |
