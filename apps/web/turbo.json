{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": [
        "AI_GATEWAY_*",
        "DATABASE_URL",
        "REDIS_URL",
        "BLOB_READ_WRITE_TOKEN",
        "LANGGRAPH_AGENT_*",
        "OPENAI_API_KEY"
      ],
      "passThroughEnv": [
        "CRON_SECRET",
        "VERCEL_SANDBOX_INTEGRATION",
        "VERCEL_OIDC_TOKEN",
        "VERCEL_PROJECT_ID",
        "VERCEL_TEAM_ID",
        "VERCEL_TOKEN"
      ]
    }
  }
}
